# Install phenopix dependencies
install.packages(c("SDMTools", "jpeg", "bcp", "strucchange"))

# install phenopix
install.packages("phenopix", repos = "http://R-Forge.R-project.org")

# news page: http://fluxnet.ornl.gov/phenopix
